import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'agenda_digital',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
